export enum RouteToNotesType {
  'active-notes' = 'Notes',
  'archived-notes' = 'Archived',
}

export enum ReduxNotesType {
  ACTIVE = 'ACTIVE',
  ARCHIVE = 'ARCHIVE',
}

export const EMPTY_FUNCTION = () => {};
